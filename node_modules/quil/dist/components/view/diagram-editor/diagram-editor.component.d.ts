import { OnInit, EventEmitter } from '@angular/core';
import * as go from 'gojs';
import { BIService } from '../../../services/bi-chart-viewer';
export declare class DiagramEditorComponent implements OnInit {
    private service;
    private diagramRef;
    model: go.Model;
    nodeSelected: EventEmitter<go.Node>;
    modelChanged: EventEmitter<go.ChangedEvent>;
    constructor(service: BIService);
    ngOnInit(): void;
}
